[package]
name = "cortex-device"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "MARU device integration for bikeboy-cortex (Wire Protocol + Volume control)"

[dependencies]
tracing = { workspace = true }
